<div class="card">
    <div class="card-header">
        Devoured
    </div>
    <div class="card-body">

        {{#each devouredList}}
            <div>{{id}}.) {{burger_name}}</div>
        {{/each }}

    </div>
</div>